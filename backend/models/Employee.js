// Define employee operations
const db = require('../database/knex');

const Employee = () => {
    TABLE_NAME = 'employee';

    return {
        // different types of searches to db
    }
}

module.exports = Employee;